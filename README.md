# 🎭 Troll Trojan

<div align="center">

![Python Version](https://img.shields.io/badge/python-3.7+-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)
![Status](https://img.shields.io/badge/status-active-success.svg)

**Инструмент для удалённого управления компьютером с функциями для розыгрышей**

[Особенности](#-особенности) • [Установка](#-установка) • [Использование](#-использование) • [Команды](#-доступные-команды)

</div>

---

## ⚠️ Дисклеймер

> **ВНИМАНИЕ!** Этот инструмент создан исключительно в образовательных целях и для безобидных розыгрышей друзей. 
> 
> - ❌ **НЕ используйте** для несанкционированного доступа к чужим компьютерам
> - ❌ **НЕ используйте** для вредоносных действий
> - ✅ **Используйте** только на своих устройствах или с явного разрешения владельца
> 
> Автор не несёт ответственности за неправомерное использование данного ПО.

---

## 🎯 Особенности

### 🖱️ Управление мышью
- Удалённое перемещение курсора
- Клики левой/правой кнопкой
- Автоматические движения по траектории
- Автокликер с настраиваемым интервалом
- **Режим реверса мыши** (инверсия направления движения)

### ⌨️ Управление клавиатурой
- Ввод произвольного текста
- Нажатие специальных клавиш (Enter, Esc, Ctrl и т.д.)
- Эмуляция комбинаций клавиш
- Перехват ввода с клавиатуры

### 📸 Мониторинг
- Скриншоты экрана в реальном времени
- Сжатие изображений для быстрой передачи
- Удалённый просмотр рабочего стола

### 🎪 Троллинг-функции
- 🎵 **Rick Roll** - автоматический запуск классического видео
- 🦠 **Fake Virus** - фальшивое окно с "заражением вирусом"
- ⚠️ **Fake Error** - имитация критической ошибки Windows
- 🚫 **Блокировка диспетчера задач** - автоматическое закрытие Task Manager
- 🔄 **Реверс мыши** - инверсия направления движения курсора

### 🌐 Сетевые возможности
- Multicast-рассылка IP-адреса для автообнаружения
- Поддержка множественных подключений
- Работа в локальной сети

---

## 📦 Установка

### Требования
- Python 3.7 или выше
- Windows (основная поддержка)

### Шаг 1: Клонирование репозитория
```bash
git clone https://github.com/Krimex1/troll-trojan.git
cd troll-trojan
```

### Шаг 2: Установка зависимостей
```bash
pip install -r requirements.txt
```

**Список необходимых библиотек:**
```
numpy
opencv-python
pyautogui
keyboard
pynput
psutil
```

---

## 🚀 Использование

### Запуск сервера (на целевом ПК)

```bash
python serveranydusk.py
```

Сервер запустится на порту `65432` и будет ожидать подключения.

### Запуск клиента (на управляющем ПК)

```bash
python superdostup.py
```

Клиент автоматически обнаружит сервер в локальной сети через multicast-рассылку.

---

## 🎮 Доступные команды

### Управление мышью

| Команда | Описание | Пример |
|---------|----------|--------|
| `move X Y` | Переместить курсор на X, Y пикселей | `move 100 50` |
| `click left/right` | Клик левой/правой кнопкой | `click left` |
| `automove [speed]` | Автоматическое движение по квадрату | `automove 0.5` |
| `autoclick [interval]` | 5 кликов с интервалом (сек) | `autoclick 1.0` |
| `reverse_mouse` | Включить/выключить реверс мыши | `reverse_mouse` |

### Управление клавиатурой

| Команда | Описание | Пример |
|---------|----------|--------|
| `type TEXT` | Ввести произвольный текст | `type Hello World` |
| `key X` | Нажать клавишу X | `key a` |
| `special_key KEY` | Нажать спец. клавишу | `special_key enter` |
| `keyboard_capture` | Начать перехват ввода | `keyboard_capture` |

**Доступные специальные клавиши:**
- `ctrl`, `shift`, `alt`
- `enter`, `esc`, `space`, `tab`
- `backspace`, `delete`
- `up`, `down`, `left`, `right`

### Системные команды

| Команда | Описание |
|---------|----------|
| `screenshot` | Получить скриншот экрана |
| `open URL` | Открыть URL в браузере |
| `shutdown` | Выключить компьютер |
| `block_taskmgr` | Вкл/выкл блокировку диспетчера задач |
| `exit` | Завершить соединение |

### Троллинг-команды 🎭

| Команда | Эффект |
|---------|--------|
| `rick` | Запустить Rick Roll в браузере 🎵 |
| `fake_virus` | Показать фальшивое окно с вирусом 🦠 |
| `fake_error` | Имитация критической ошибки Windows ⚠️ |

---

## 🏗️ Архитектура

```
┌─────────────────────┐         ┌─────────────────────┐
│   Клиент            │ ◄────► │    Сервер           │
│  (superdostup.py)   │  TCP    │ (serveranydusk.py)  │
└─────────────────────┘ 65432   └─────────────────────┘
         │                                  │
         │ Multicast                        │
         │ Discovery                        │ Локальное
         │ UDP:45631                        │ выполнение
         └──────────────────────────────────┘
```

### Компоненты

- **RemoteControlServer** - основной серверный класс
- **Multicast Discovery** - автообнаружение в сети
- **Command Processor** - обработчик команд
- **Background Monitors** - фоновые мониторы (например, Task Manager)

---

## 🔧 Конфигурация

### Изменение порта сервера

В `serveranydusk.py`:
```python
server = RemoteControlServer(host='0.0.0.0', port=65432)
```

### Изменение адреса multicast

В `multicast_send()`:
```python
sock.sendto(local_ip.encode('utf-8'), ("172.16.79.23", 45631))
```

---

## 🛡️ Безопасность

### Рекомендации:
- 🔒 Используйте **только в доверенных сетях**
- 🚫 **Не открывайте** порт во внешний интернет
- 🔐 Рассмотрите добавление **аутентификации** для продакшн-использования
- 📝 Всегда **логируйте** подключения и действия

### Защита:
```python
# Пример добавления простой аутентификации
def authenticate(client_socket):
    password = "your_secret_password"
    received = client_socket.recv(1024).decode()
    return received == password
```

---

## 📋 TODO

- [ ] Добавить шифрование трафика (SSL/TLS)
- [ ] Реализовать систему аутентификации
- [ ] Поддержка Linux/macOS
- [ ] Передача файлов
- [ ] Видеопоток в реальном времени
- [ ] Web-интерфейс управления
- [ ] Логирование всех действий

---

## 🤝 Участие в разработке

Если у вас есть идеи или вы нашли баг:

1. Форкните репозиторий
2. Создайте ветку (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

---

## 👨‍💻 Автор

**Krimex1**

- GitHub: [@Krimex1](https://github.com/Krimex1)

---

## ⭐ Поддержка проекта

Если проект оказался полезным, поставьте звёздочку ⭐

---

<div align="center">

**Сделано с 💀 для розыгрышей друзей**

</div>